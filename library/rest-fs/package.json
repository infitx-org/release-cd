{
    "name": "@infitx/rest-fs",
    "version": "1.2.3",
    "description": "Rest filesystem plugin",
    "main": "index.js",
    "scripts": {
        "build": "true",
        "ci-unit": "JEST_JUNIT_OUTPUT_DIR=coverage jest --ci --reporters=default --reporters=jest-junit --outputFile=./coverage/junit.xml;allure generate",
        "watch": "node --watch .",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage"
    },
    "dependencies": {
        "http-proxy-middleware": "^3.0.3",
        "ws": "^8.18.0"
    },
    "devDependencies": {
        "allure": "^3.2.0",
        "allure-jest": "^3.4.5",
        "@hapi/hapi": "^21.3.12",
        "jest": "^30.2.0",
        "jest-environment-node": "^30.2.0",
        "jest-junit": "16.0.0"
    },
    "author": "Kalin Krustev",
    "license": "Apache-2.0"
}